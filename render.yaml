services:
  - type: web
    name: school-management
    env: python
    buildCommand: pip install -r requirements.txt && python manage.py migrate
    startCommand: gunicorn school_management.wsgi
    envVars:
      - key: DJANGO_DEBUG
        value: "False"
      - key: DJANGO_SECRET_KEY
        value: django-insecure-)ti@p9qago*$54_z%qwp5%wcv(js9z1mfp%8le($*!0jweq_yn
